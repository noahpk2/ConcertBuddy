<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ConcertBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="OutlinedTextView">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:outlineProvider">bounds</item>
        <item name="android:elevation">2dp</item> <!-- Optional: Add elevation for shadow effect -->
        <item name="android:padding">4dp</item> <!-- Optional: Add padding to give space between text and outline -->
    </style>


    <style name="Theme.ConcertBuddy" parent="Base.Theme.ConcertBuddy" />
</resources>